import ChatService from "../services/chat-service.js";
const svc = new ChatService();
const gp  = (req) => ({ performedBy: req.user._id, performedByName: req.user.name || req.user.username || req.user.email || "Admin" });

// ── Channels ───────────────────────────────────────────────────
const createChannel   = async (req,res) => { try { const {performedBy,performedByName}=gp(req); const d=await svc.createChannel(req.body,performedBy,performedByName); res.status(201).json({success:true,message:"Channel created.",data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getAllChannels   = async (req,res) => { try { const d=await svc.getAllChannels(); res.status(200).json({success:true,data:d,count:d.length}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getMyChannels   = async (req,res) => { try { const d=await svc.getChannelsForUser(req.user._id); res.status(200).json({success:true,data:d,count:d.length}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getOrCreateDM   = async (req,res) => { try { const {targetUserId,targetUserName}=req.body; if(!targetUserId) return res.status(400).json({success:false,message:"targetUserId required."}); const myName=req.user.name||req.user.username||req.user.email||"Admin"; const d=await svc.getOrCreateDM(req.user._id,targetUserId,myName,targetUserName||"User"); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const addMember       = async (req,res) => { try { const {userId}=req.body; const d=await svc.addMember(req.params.id,userId); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const deleteChannel   = async (req,res) => { try { const {performedBy,performedByName}=gp(req); await svc.deleteChannel(req.params.id,performedBy,performedByName); res.status(200).json({success:true,message:"Channel deleted."}); } catch(e){res.status(e.message.includes("not found")?404:500).json({success:false,message:e.message});} };

// ── Messages ───────────────────────────────────────────────────
const sendMessage     = async (req,res) => { try { const {content,type,attachments,replyTo,mentions}=req.body; if(!content&&!attachments?.length) return res.status(400).json({success:false,message:"content required."}); const name=req.user.name||req.user.username||req.user.email||"Admin"; const d=await svc.sendMessage(req.params.channelId,req.user._id,name,req.user.roles?.[0]||"admin",content,type,attachments,replyTo,mentions); res.status(201).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getMessages     = async (req,res) => { try { const {limit,before}=req.query; const d=await svc.getMessages(req.params.channelId,parseInt(limit)||50,before); res.status(200).json({success:true,data:d,count:d.length}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const markRead        = async (req,res) => { try { await svc.markRead(req.params.channelId,req.user._id); res.status(200).json({success:true,message:"Marked as read."}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const deleteMessage   = async (req,res) => { try { const d=await svc.deleteMessage(req.params.id,req.user._id); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const addReaction     = async (req,res) => { try { const {emoji}=req.body; if(!emoji) return res.status(400).json({success:false,message:"emoji required."}); const d=await svc.addReaction(req.params.id,emoji,req.user._id); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const searchMessages  = async (req,res) => { try { const {q}=req.query; if(!q) return res.status(400).json({success:false,message:"q required."}); const d=await svc.searchMessages(req.params.channelId,q); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const sendAnnouncement= async (req,res) => { try { const {channelId,content}=req.body; if(!channelId||!content) return res.status(400).json({success:false,message:"channelId and content required."}); const {performedBy,performedByName}=gp(req); const name=req.user.name||req.user.username||req.user.email||"Admin"; const d=await svc.sendAnnouncement(channelId,req.user._id,name,content,performedBy,performedByName); res.status(201).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };

export default { createChannel, getAllChannels, getMyChannels, getOrCreateDM, addMember, deleteChannel, sendMessage, getMessages, markRead, deleteMessage, addReaction, searchMessages, sendAnnouncement };
