import PerformanceService from "../services/performance-service.js";
const svc = new PerformanceService();
const gp  = (req) => ({ performedBy: req.user._id, performedByName: req.user.name || req.user.username || req.user.email || "Admin" });

const generate       = async (req,res) => { try { const { employeeId } = req.params; const { month, year } = req.body; if (!month||!year) return res.status(400).json({success:false,message:"month and year required."}); const d = await svc.generateForEmployee(employeeId, parseInt(month), parseInt(year)); res.status(200).json({success:true,message:"Performance generated.",data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getForEmployee = async (req,res) => { try { const d = await svc.getForEmployee(req.params.employeeId); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getForPeriod   = async (req,res) => { try { const { employeeId } = req.params; const { month, year } = req.query; if (!month||!year) return res.status(400).json({success:false,message:"month and year required."}); const d = await svc.getForPeriod(employeeId, parseInt(month), parseInt(year)); if (!d) return res.status(404).json({success:false,message:"No performance record for this period."}); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getAllForPeriod = async (req,res) => { try { const { month, year } = req.query; if (!month||!year) return res.status(400).json({success:false,message:"month and year required."}); const d = await svc.getAllForPeriod(parseInt(month), parseInt(year)); res.status(200).json({success:true,data:d,count:d.length}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const addManagerScore= async (req,res) => { try { const { employeeId } = req.params; const { month, year, ...scores } = req.body; const { performedBy, performedByName } = gp(req); const d = await svc.addManagerScore(employeeId, parseInt(month), parseInt(year), scores, performedBy, performedByName); res.status(200).json({success:true,message:"Manager score added.",data:d}); } catch(e){res.status(e.message.includes("not found")?404:500).json({success:false,message:e.message});} };
const getTopPerformers=async(req,res)=>{ try { const {month,year} = req.query; const d = await svc.getTopPerformers(parseInt(month||new Date().getMonth()+1), parseInt(year||new Date().getFullYear())); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };
const getTrend       = async (req,res) => { try { const d = await svc.getTrend(req.params.employeeId, parseInt(req.query.months||6)); res.status(200).json({success:true,data:d}); } catch(e){res.status(500).json({success:false,message:e.message});} };

export default { generate, getForEmployee, getForPeriod, getAllForPeriod, addManagerScore, getTopPerformers, getTrend };
